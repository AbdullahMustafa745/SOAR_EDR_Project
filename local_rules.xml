{
  "trigger": "EDR Alert Webhook",
  "steps": [
    {
      "action": "Validate An Alert",
      "condition": "severity == high"
    },
    {
      "action": "Isolate Endpoint",
      "api_call": "POST /v1/isolate"
    },
    {
      "action": "Send Slack Notification"
    }
  ]
}
